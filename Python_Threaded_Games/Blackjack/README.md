# Network Blackjack

Two clients play separate hands against the same dealer.

Host runs `server.py`.
Both players run `client.py`.

Default port: 5002.

Note: this is a learning implementation demonstrating sockets and threads.
